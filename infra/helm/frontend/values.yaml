replicaCount: 1

image:
  registry: ghcr.io/pavelkrasko
  client:
    name: client
    tag: "1.0.1"
  pullPolicy: IfNotPresent

backend:
  serviceName: issuetracker-server
  servicePort: 8080

service:
  port: 80
