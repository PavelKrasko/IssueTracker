apiVersion: v1
kind: Service
metadata:
  name: issuetracker-server
  labels:
    app: issuetracker-server
spec:
  selector:
    app: issuetracker-server
  ports:
    - name: http 
      port: 8080
      targetPort: 8080
